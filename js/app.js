console.log('Javascript Working!');

$('body').prepend('<header class="searchbar wrapper"><input type="search" name="searchbar" placeholder="Search"></header>');

lightbox.option({
  'alwaysShowNavOnTouchDevices': true,
  'showImageNumberLabel': false,
  'disableScrolling': true,
});
